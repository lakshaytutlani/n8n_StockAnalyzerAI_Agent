
	- We need Telegram, Chart, Airtable
	- Telegram:
			§ Look for botfather
			§ Need to hit start first
			§ Type:
				□ /start
				□ /newbot
			§ Copy the API key
		○ Add the API key to telegram
		○ Click execute the flow
	- Airtable
		○ Create a table with name as a column
		○ Change api key
	- In trading agent:
		○ Change the API key in geturl
	- Webhook copy into run agent
